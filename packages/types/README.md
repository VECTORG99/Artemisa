# @artemisa/types

Tipos TypeScript compartidos entre los paquetes del monorepo.

## Consumo

Importado como workspace dependency por frontend y otros paquetes. No requiere publicacion en npm.

## Build

```bash
npm run build
```

Genera los tipos compilados en `dist/`.
